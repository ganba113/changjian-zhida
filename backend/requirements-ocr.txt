# OCR 可选依赖（RapidOCR onnxruntime 后端）
# 说明：
# - 本文件由 deploy/download_wheels.py 下载对应 aarch64 wheel 后，在 install.sh 中离线安装；
# - 未安装时 /api/ocr 返回 503 友好提示，不影响其他功能；
# - onnxruntime / numpy 已在主 requirements.txt 中（复用向量检索依赖），此处不重复列出；
# - 用 opencv-python-headless 替代 opencv-python：服务器无 GUI 环境，headless 不依赖 libGL 等系统库；
# - 安装时必须加 --no-deps：rapidocr_onnxruntime 声明依赖 opencv-python（非 headless），二者是不同包名，
#   pip 无法自动替代，不加 --no-deps 会因找不到 opencv-python 报 "from versions: none"。install.sh 已处理。
#
# 版本锁定原因（勿随意升级）：
#   opencv-python-headless==4.9.0.80  4.10+/5.x 依赖 numpy 2.x，与 numpy 1.24.4 冲突
#   shapely==2.0.5                    2.1.x 要求 Python>=3.10（服务器 3.9.9）；rapidocr 要求 !=2.0.4
#   pyclipper==1.3.0.post5            最新 1.4.0 未提供 aarch64 wheel
#   Pillow==10.4.0                    12.x 无 cp39 wheel
#   PyMuPDF==1.26.0                   1.26.1+ 改用 manylinux_2_28(glibc 2.28+)；1.26.0 是 manylinux2014 最新自包含版
rapidocr_onnxruntime==1.4.4
opencv-python-headless==4.9.0.80
pyclipper==1.3.0.post5
shapely==2.0.5
six==1.16.0
PyYAML==6.0.1
Pillow==10.4.0
tqdm==4.66.5
PyMuPDF==1.26.0
